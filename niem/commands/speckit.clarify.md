> **NIEM preset:** Before executing this command, read
> `.specify/presets/niem/templates/niem-guidance.md`; stop and report its path if
> unavailable. Apply the NIEM guidance below within the existing stage, not after
> completion. Preserve all base prerequisites, paths, permissions, hooks, and approvals.

{CORE_TEMPLATE}

## NIEM Clarification Guidance

- Within the normal ambiguity scan, prioritize unclear exchange boundaries,
  semantic ownership, required information, identity, code meanings, and
  producer/consumer compatibility obligations.
- Establish whether NIEM is mandatory, proposed, or irrelevant. Preserve an
  existing release constraint; do not treat the preset's reference baseline as
  an answered question.
- Clarify acceptance evidence and behavior for missing/unknown values, invalid
  messages, and unsupported partner versions where these change requirements.
- Ask within the base command's existing question budget and interaction rules;
  reuse prior answers. Do not introduce another questionnaire.
- Integrate answers into the current spec and clarification log only as the base
  command allows. Defer component/schema design to planning and do not turn
  unresolved candidates into official NIEM definitions.
